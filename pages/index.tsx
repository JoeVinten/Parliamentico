import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Parliamentico</title>
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main>
        <h1>Parliamentico</h1>
      </main>
    </div>
  );
};

export default Home;
